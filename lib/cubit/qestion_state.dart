class QuestionState {
  final bool isLoading;
  final bool isSuccess;
  final bool isError;

  QuestionState({
    this.isLoading = false,
    this.isSuccess = false,
    this.isError = false,
  });
}
